name: build msyh
on:
  push:
  schedule:
    - cron: '0 0 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    container: archlinux:latest
    steps:
    - name: Install dependencies
      run: |
        echo "/github/home/.local/bin" >> $GITHUB_PATH
        echo "/github/home/.deno/bin" >> $GITHUB_PATH
        pacman -Syu --noconfirm git fontforge curl p7zip jq python-pip unzip
        pip3 install --user afdko
        curl -fsSL https://deno.land/install.sh | sh
        realpath ~/.deno/bin/deno
        ~/.deno/bin/deno help
    - uses: actions/checkout@v3
    - name: download_judou_sans
      run: deno run --config ./deno.json --allow-all --v8-flags=--max-old-space-size=8192 download_judou_sans.ts
    - name: Run script
      run: |
        mkdir out
        echo "----------------------"
        deno run --config ./deno.json --allow-all --v8-flags=--max-old-space-size=8192 yahei.js
        echo "----------------------"
        deno run --config ./deno.json --allow-all --v8-flags=--max-old-space-size=8192 simsun.ts
        echo "----------------------"
        deno run --config ./deno.json --allow-all --v8-flags=--max-old-space-size=8192 droid-sans-mono.ts
        echo "----------------------"
        ls out/*
    - name: Compress font
      run: |
        cd out
        7z a ../YaHei.zip *
    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: YaHei
        path: YaHei.zip
    - name: Upload release
      uses: ncipollo/release-action@v1
      with:
        token: "${{ secrets.GITHUB_TOKEN }}"
        tag: "JudouSans-${{env.JUDOU_SANS_VERSION}}-UI-${{env.JUDOU_SANS_UI_VERSION}}-I.MING-${{env.I_MING_VERSION}}"
        artifacts: YaHei.zip
        allowUpdates: true
